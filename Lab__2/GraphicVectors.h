#ifndef GRAPHICVECTORS_H
#include <fstream>
#include <QStandardItemModel>
#include <handler.h>
#define GRAPHICVECTORS_H

void setGraphicVectors(vector<Record> records, QVector<double> *x, QVector<double> *y, int col);

#endif // GRAPHICVECTORS_H
